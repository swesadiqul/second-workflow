name: Manual Workflow

on:
    workflow_dispatch:

jobs:
  manual-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        run: |
         echo "Hello, Python CI"
         echo "I'm running a manual workflow"